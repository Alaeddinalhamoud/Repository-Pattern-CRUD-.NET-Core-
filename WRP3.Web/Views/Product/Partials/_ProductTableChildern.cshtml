@using WRP3.Domain.Entities;
@model IEnumerable<Product>

@foreach (var item in Model ?? new List<Product>())
{
    <tr>
        <td>
            @item?.Id
        </td>
        <td>
            @item?.Name
        </td>
        <td>
            @item?.Created.ToString("d")
        </td>
        <td>
            🟢  or 🟡
        </td>
        <td>
            <a asp-action="ProductTestCases" asp-route-id="@item.Id">Test Cases</a>
        </td>       
    </tr>
}